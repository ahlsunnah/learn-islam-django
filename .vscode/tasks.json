{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Kill processes running on port 8000",
      "type": "shell",
      "command": "sudo lsof -t -i tcp:8000 | xargs kill -9"
    }
  ]
}
